import { Injectable } from "@angular/core";

@Injectable({ providedIn: 'root' })
export class TaskMapper {
  
  // Domain Entity → DTO
  

  // DTO → Domain Entity
  

  // API Response → DTO
  
}